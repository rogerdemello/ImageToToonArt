# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Image Processing - Install from PyPI wheels (Windows compatible)
# Use these specific versions that are known to work
opencv-python-headless==4.8.1.78
Pillow

# Deep Learning (Optional - for AI-based conversion)
# Uncomment below if you want AI features (anime, watercolor styles)
# torch
# torchvision

# Utilities
python-dotenv
aiofiles
